# Rails with JWT authentication

This repo was created to learn how to use JWT auth in Rails.

* install dependencies
```sh
    $ bundle install
```

* run server
```sh
    $ rails s
```

Tools I used:
* Insomnia
